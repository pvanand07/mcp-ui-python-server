# MCP UI Python Server - HTTP (SSE) transport
# Run: docker compose up --build
# Server runs as: python server.py --http --port 3000 (host port 20022 → container 3000)
services:
  mcp-ui-server:
    build: .
    ports:
      - "20022:3000"